## Migrations
To create new SQM migration use script 
`./scripts/new-migration.sh migration-name`

it will automatically create new .sql file into correct folder with name that specifies executing migrations in correct order.